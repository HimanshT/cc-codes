// Median of two sorted arrays of different sizes
// approach: first of all create a single array,that contains both the arrays
// next find the median using simple formula
// to create a sorted array from two arrays ,you can merge the arrays and then sort.